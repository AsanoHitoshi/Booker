<header>
	<% if flash[:notice] %>
		<div class="flash">
			<%= flash[:notice]%>
		</div>
	<% end %>
</header>
<body>
	<div>
		<p>Title: <%= @book.title %></p>
		<p>Body: <%= @book.body %></p>
		<%= link_to "edit",edit_book_path(@book) %>
		<%= link_to "back","/books" %>
	</div>
</body>