<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>

	<p>title</p>
	<%= f.text_field :title %>
	<p>body</p>
	<%= f.text_area :body %><br>
	<%= f.submit 'Update Book' %>

<% end %>
<p>
	<%= link_to "Show",book_path(@book) %>
	|
	<%= link_to "Back","/books" %>
</p>